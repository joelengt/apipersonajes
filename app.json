{
    "environments": {
      "test": {
        "scripts": {
          "test-setup": "npm install jest --global",
          "test": "npm run test"
        },
        "env": {
            "CROMLU": "secret"
        }
      }
    }
  }